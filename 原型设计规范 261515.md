# 微信小程序原型设计规范

> **用途**：本文档规定了止观实验室在进行微信小程序 Web 原型设计时的所有标准要求。
> 每次新建原型项目时，需严格按照本文档执行。
>
> **最后更新**：2026-05-16

---

## 目录

1. [项目结构](#1-项目结构)
2. [Shell 布局：三栏结构](#2-shell-布局三栏结构)
2.1. [页面树：全量暴露原则](#21-页面树全量暴露原则)
3. [手机框架规格](#3-手机框架规格)
4. [TDesign 设计规范](#4-tdesign-设计规范)
5. [黑白灰配色体系](#5-黑白灰配色体系)
6. [右侧交互说明面板](#6-右侧交互说明面板)
7. [页面内导航机制](#7-页面内导航机制)
8. [共享样式文件 td.css](#8-共享样式文件-tdcss)
9. [页面文件规范](#9-页面文件规范)
10. [交付检查清单](#10-交付检查清单)
11. [层级页面、变体与侧边栏注册](#11-层级页面变体与侧边栏注册)

---

## 1. 项目结构

```
项目名称/
├── PRD.md                        # 产品需求文档（必须）
└── app/
    ├── index.html                # Shell 入口：三栏布局（必须第一步创建）
    ├── pages/                    # 各页面 HTML
    │   ├── home.html
    │   ├── stats.html
    │   └── ...
    └── assets/
        ├── js/
        │   └── vue.js            # 本地 Vue 2.6.14
        └── td.css                # 共享设计 Token 样式（必须）
```

**命名规范**：

| 页面类型 | 文件名示例 |
|---------|----------|
| 主页 / Tab 页 | `home.html`、`stats.html` |
| 列表页 | `checkin-list.html` |
| 详情页 | `checkin-detail.html` |
| 表单页 | `create-checkin.html` |
| 内容页 | `checkin-feed.html`、`my-history.html` |
| 落地页 | `invite-landing.html` |

---

## 2. Shell 布局：三栏结构

`app/index.html` 是原型的唯一入口，采用**三栏布局**，不展示页面内容，只作为预览容器。

```
┌── 顶部工具栏（44px，黑底）──────────────────────────────────────────┐
│  项目名  ·  线框原型 · 共 N 个界面        ‹ ›  [当前页名]             │
├── 左侧（320px）───┬── 中间（弹性）────────────┬── 右侧（320px）──────┤
│  分组标题          │                           │  交互说明              │
│  P01 业务流程      │   手机框架                 │  （内容多时可滚动）    │
│  分组标题          │   ┌──────────┐             │  · 交互行为（列表）    │
│  P02 今日打卡      │   │  iframe  │             │                       │
│  P03 空态·无任务   │   │  (页面)  │             │  页面跳转（可点击）    │
│  P04 封闭模式确认  │   └──────────┘             │                       │
│  …（二级/三级界面   │                           │                       │
│    均单独成行）    │   点阵背景                │                       │
└───────────────────┴───────────────────────────┴───────────────────────┘
```

### 顶部工具栏规范

| 元素 | 规格 |
|------|------|
| 背景色 | `#1a1a1a` |
| 高度 | `44px` |
| 项目标题 | 白色 `13px 700` |
| 描述标签 | `#777 11px` |
| 后退/前进按钮 | `26×26px`，记录导航历史 |
| 当前页名 | `max-width 140px`，超出截断 |

### 左侧页面列表规范

| 元素 | 规格 |
|------|------|
| 宽度 | `320px`（与参考实现对齐；窄屏项目可降至 `192px`，但须保证树节点文案可读） |
| 背景色 | `#ffffff` |
| 右边框 | `1px solid #e8e8e8` |
| Section 标题 | `10px 700 uppercase letter-spacing:1.5px #bbb` |
| 未选中行 | `font-size:12.5px color:#444` |
| 选中行 | `background:#f5f5f5 color:#111 font-weight:600 border-left:3px solid #111` |
| 编号 | 右侧 **`P01`～`Pnn` 连续编号**，覆盖含空态、子视图在内的**每一个**可预览界面；`10px #ccc` |

### 2.1 页面树：全量暴露原则

**目标**：开发人员打开原型后，能在左侧**一次性看到全部界面**（含二级流程页、详情页内子视图、空态变体等），**单击即可预览**，无需从主界面多次点入才能「挖」到深层页面。

| 规则 | 说明 |
|------|------|
| **禁止隐藏叶子节点** | 凡具备独立 `pages/*.html` 或通过 **query 参数**区分的可预览状态，均须在左侧注册为一行；不得以「仅能通过父页跳转进入」为理由省略树节点。 |
| **分组 + 深度缩进** | 可用 `section`（分组标题）组织业务模块；层级关系用 `depth`（0/1/2…）仅做**左侧缩进与辅助连线**，不改变交互权重。 |
| **统一样式权重** | 二级、三级节点与一级节点使用**同一套**行高、字号、选中态（左侧 `3px #111` 高亮条）；不得再用弱化色、无编号等方式把深层页面做成「次要条目」。 |
| **顶部统计** | 工具栏文案使用 **「共 N 个界面」**，`N` = `PAGES` 中全部注册行数（含变体），而非仅 Tab 父页数量。 |
| **排序建议** | 同一分组内顺序：**主界面 → 衍生子流程页 → 同一文档下的参数变体（空态、未登录等）**，便于扫读。 |

### 中间画布规范

| 元素 | 规格 |
|------|------|
| 背景 | `#e4e4e4` + 点阵纹理（`radial-gradient`，`22px × 22px`） |
| 手机标签 | 手机上方，`11px #888` |

### 右侧面板规范（见第 6 节）

> ⚠️ 右侧面板宽度为 **320px**（比左侧宽，因交互说明文字较多）。内容区已启用 `overflow-y: auto`，内容超出时可滚动。

---

## 3. 手机框架规格

### 尺寸

| 属性 | 规格 |
|------|------|
| 外框尺寸 | `356px × 752px` |
| 内屏尺寸 | `332px × 728px` |
| 外框圆角 | `44px` |
| 内屏圆角 | `33px` |
| 外框背景 | `#111` |
| 内屏背景 | `#ffffff` |

### 外观细节

```css
.device {
  box-shadow:
    0 0 0 1px #222,               /* 边框描边 */
    0 32px 80px rgba(0,0,0,0.40), /* 投影 */
    inset 0 0 0 1px rgba(255,255,255,0.05); /* 内高光 */
}
```

- 顶部刘海：`120px × 28px`，`border-radius: 0 0 18px 18px`
- 左侧音量键：两个，各 `3px × 32px`
- 右侧电源键：`3px × 54px`

### iframe 设置

```html
<iframe :src="'pages/' + key + '.html'" 
  style="width:332px; height:728px; border:none; display:block;">
</iframe>
```

> ⚠️ iframe 的 `width` 和 `height` 直接写死为实际像素值，不用 `100%`，以确保移动端渲染一致性。

---

## 4. TDesign 设计规范

原型遵循 [TDesign 微信小程序设计规范](https://tdesign.tencent.com/miniprogram/overview)。

### 核心组件规范

| 组件 | TDesign 规格 |
|------|-------------|
| **NavBar** | 状态栏 `20px` + 内容区 `44px`；白底；标题居中 `18px 600`；左侧返回箭头 `44×44px` 触摸区 |
| **TabBar** | 高度 `50px`；白底；上边框 `1px #E5E5E5`；图标 `20px` + 标签 `10px`；激活态黑色 |
| **Cell（列表项）** | 最小高度 `56px`；水平 padding `16px`；底部分隔线 `1px #E5E5E5`；右侧箭头 `12px` |
| **Button** | 主按钮 `44px` 高；`border-radius: 8px`；黑色填充白字 |
| **Input** | `border-radius: 6px`；边框 `1px #E5E5E5`；聚焦态边框加深 |
| **Card** | `border-radius: 8px`；`box-shadow: 0 1px 6px rgba(0,0,0,0.06)` |
| **底部弹层** | `border-radius: 12px 12px 0 0`；顶部拖拽条 `40×4px` |
| **Toast** | 半透明黑底 `rgba(0,0,0,0.75)`；`border-radius: 8px`；居中浮层 |
| **Segmented Control** | 灰色底 `#EBEBEB`；选中白底 + `shadow-1` |
| **Tag** | `border-radius: 3px`；`12px` 字号 |

### 页面背景

| 用途 | 颜色 |
|------|------|
| 页面背景 | `#F5F5F5` |
| 卡片背景 | `#FFFFFF` |
| 输入框背景 | `#F8F8F8` |

### 间距遵循 4pt 基础网格

| Token | 值 |
|-------|----|
| `--td-sp-xs` | `4px` |
| `--td-sp-s` | `8px` |
| `--td-sp-m` | `12px` |
| `--td-sp-l` | `16px`（页面左右 padding） |
| `--td-sp-xl` | `20px` |
| `--td-sp-2xl` | `24px` |

---

## 5. 黑白灰配色体系

> **设计理念**：原型使用纯黑白灰配色，避免颜色对 UI 设计师产生先入为主的心理暗示，最大化后续设计师的创作空间。

### 颜色 Token

| Token | 值 | 用途 |
|-------|----|------|
| `--td-brand` | `#111111` | 主要强调色（按钮、激活态、边框高亮） |
| `--td-brand-light` | `#F0F0F0` | 主色浅色背景 |
| `--td-text-1` | `#111111` | 主要文字 |
| `--td-text-2` | `#555555` | 次要文字 |
| `--td-text-3` | `#999999` | 辅助/占位文字 |
| `--td-text-4` | `#CCCCCC` | 禁用文字 |
| `--td-border` | `#E5E5E5` | 分隔线、边框 |
| `--td-bg-page` | `#F5F5F5` | 页面背景 |
| `--td-bg-card` | `#FFFFFF` | 卡片背景 |

### Hero / 渐变区域

所有需要渐变背景的 Hero 区域（个人中心、打卡项详情、邀请落地页等），统一使用：

```css
background: linear-gradient(160deg, #1a1a1a 0%, #333333 100%);
```

**例外情况**：AI 自动生成的封面图预览卡允许使用多套深色渐变主题（靛蓝、紫绿、炭灰、深绿、暮橙），用于模拟 AI 生成封面的视觉多样性。这类渐变仅限封面预览组件内使用，不用于页面整体 UI。

### 状态表达方式

彩色用于状态区分时，改用以下替代方案：

| 原彩色状态 | 黑白灰替代方案 |
|-----------|--------------|
| 绿色 = 成功/已完成 | 深灰图标 + `✓` 符号 |
| 橙色 = 警告/待处理 | 边框加粗 + 说明文字 |
| 红色 = 错误 | 黑色加粗边框 + 错误文字 |
| 蓝色 = 链接/信息 | 下划线 + 字重加粗 |

### 进度 / 热力图

| 元素 | 配色 |
|------|------|
| 进度条底色 | `#E8E8E8` |
| 进度条填充 | `#111111` |
| 热力图 0 级 | `#F5F5F5 + 1px border #E5E5E5` |
| 热力图 1 级 | `#D0D0D0` |
| 热力图 2 级 | `#888888` |
| 热力图 3 级 | `#1a1a1a` |

---

## 6. 右侧交互说明面板

### 定位

右侧面板**只展示「交互说明」**，不展示设计规范、Token 信息、产品描述等内容。目的是在查看页面时能快速了解可交互元素的行为。

### 面板结构

```
┌─────────────────────────────┐
│  🏠 今日打卡   [交互说明]    │  ← 页面标题 + 徽章
├─────────────────────────────┤
│  ✦ 交互行为                  │
│  · 点击「打卡」→ Sheet 上滑  │  ← 每条交互，dot + 文字
│  · 姓名为空→ 报错提示        │
│  · ...                       │
├─────────────────────────────┤
│  ⬡ 页面跳转                  │
│  [→ 统计页]                  │  ← 可点击跳转
│  [→ 个人中心]                │
└─────────────────────────────┘
```

### 交互说明写作规范

- **格式**：`[触发方式] → [响应结果]`
- **触发方式**：点击 / 长按 / 滑动 / 输入 / 选择 / 失焦
- **响应结果**：描述视觉反馈，必要时加时间参数（如`延迟 1.2s`、`动画 0.25s`）
- **每页交互条数**：建议 4～10 条，聚焦关键交互，不需要穷举每个元素

**示例**：
```
点击「打卡」按钮 → 底部 Sheet 上滑（0.25s ease-out）
姓名为空点击「确认打卡」→ 输入框描边变黑 + Toast 提示
打卡成功 → 关闭 Sheet，全屏成功遮罩展示连续天数
点击「生成海报」→ 跳转海报页（延迟 0s）
```

### 页面跳转链接

右侧面板的「页面跳转」区块中每个链接**可点击**，点击后直接在中间手机框切换页面（通过 Vue 数据驱动）。

---

## 7. 页面内导航机制

所有页面使用 `postMessage` 与 Shell（`index.html`）通信，实现页面跳转。

### 导航函数（每个页面必须包含）

```javascript
function _nav(page) {
  if (window.parent !== window) {
    window.parent.postMessage({ navigate: page }, '*');
  }
}
```

### Shell 监听（index.html 中）

```javascript
window.addEventListener('message', e => {
  if (e.data && e.data.navigate) this.go(e.data.navigate);
});
```

### 导航历史

Shell 维护导航历史栈，顶部工具栏的 `‹`（后退）和 `›`（前进）按钮可在历史中穿梭。

### 跳转规范

| 行为 | 实现方式 |
|------|---------|
| 页面导航 | `_nav('page-key')` |
| 延迟跳转（如表单提交后） | `setTimeout(() => _nav('target'), 1200)` |
| 微信分享/系统 API | `showT('提示文字')` + Toast 模拟 |
| 外部链接 | 不实现，用 Toast 说明 |

### 页面 key 命名表

| 页面 | key | 类型 |
|------|-----|------|
| 今日打卡 | `home` | 父页面 |
| 封闭模式·确认打卡 | `checkin-closed` | 独立确认页（归属「今日打卡」） |
| 开放模式·确认打卡 | `checkin-open` | 独立确认页（归属「今日打卡」） |
| 个人中心 | `profile` | 父页面 |
| 打卡项列表 | `checkin-list` | 父页面 |
| 打卡项详情 | `checkin-detail` | 父页面 |
| 发起打卡 | `create-checkin` | 父页面 |
| 生成海报 | `poster` | 父页面 |
| 全部打卡动态 | `checkin-feed` | 父页面 |
| 我的打卡记录 | `my-history` | 父页面 |
| 邀请落地页 | `invite-landing` | 父页面 |

> - 新增页面或新的 **query 变体**时，同时在 `index.html` 的 `PAGES` 数组和 `NOTES` 对象中注册。
> - **二级 / 三级界面**：在 `PAGES` 中为每一行设置 `section`（分组）与 `depth`（树深度，用于左侧 padding）；**每一行均分配连续 `pNum`**，不再使用 `isChild: true` 弱化展示（参见 [§2.1](#21-页面树全量暴露原则)）。

---

## 8. 共享样式文件 td.css

所有页面通过相对路径引入：

```html
<link rel="stylesheet" href="../assets/td.css">
```

### 可用 CSS 类速查

| 类名 | 用途 |
|------|------|
| `.td-navbar` | 导航栏容器 |
| `.td-statusbar` | 状态栏占位（`20px`） |
| `.td-navbar-content` | 导航栏内容行（`44px`） |
| `.td-navbar-title` | 居中标题 |
| `.td-navbar-back` | 返回按钮区 |
| `.td-navbar-action` | 右侧操作区 |
| `.td-tabbar` | 底部 TabBar |
| `.td-tab-item` | Tab 单项 |
| `.td-tab-item.active` | Tab 激活态 |
| `.td-cell` | 列表项 |
| `.td-cell-icon` | 列表左图标 |
| `.td-cell-body` | 列表内容区 |
| `.td-cell-title` | 列表主标题 |
| `.td-cell-note` | 列表副标题 |
| `.td-cell-right` | 列表右侧（箭头等）|
| `.td-card` | 卡片容器 |
| `.td-card-header` | 卡片标题行 |
| `.td-card-body` | 卡片内容区 |
| `.td-btn` | 按钮基础 |
| `.td-btn-primary` | 主要按钮（黑色填充）|
| `.td-btn-outline` | 线框按钮 |
| `.td-btn-gray` | 灰色次要按钮 |
| `.td-btn-block` | 全宽按钮 |
| `.td-btn-sm` | 小尺寸按钮（`32px`）|
| `.td-input` | 输入框 |
| `.td-textarea` | 多行输入框 |
| `.td-input-label` | 输入框标签 |
| `.td-char-count` | 字数统计 |
| `.td-tag` | 标签基础 |
| `.td-tag-success` | 标签-成功（浅灰）|
| `.td-tag-warning` | 标签-警告（浅灰）|
| `.td-tag-default` | 标签-默认（浅灰）|
| `.td-segment` | 分段控制器容器 |
| `.td-segment-item` | 分段控制器单项 |
| `.td-progress-wrap` | 进度条容器 |
| `.td-progress-fill` | 进度条填充 |
| `.td-overlay` | 遮罩层（50% 透明黑）|
| `.td-sheet` | 底部弹层 |
| `.td-sheet-handle` | 弹层拖拽条 |
| `.td-sheet-header` | 弹层标题行 |
| `.td-sheet-body` | 弹层内容区 |
| `.td-toast` | Toast 提示 |
| `.td-divider` | 分隔线（`1px`）|
| `.td-divider-space` | 分区间距（`8px` 灰条）|
| `.td-hero` | Hero 区域（黑色渐变）|
| `.td-hero-text` | Hero 白色文字 |
| `.td-hero-muted` | Hero 半透明文字 |
| `.td-hero-badge` | Hero 徽章 |
| `.td-hero-card` | Hero 内统计小卡片 |
| `.no-sb` | 隐藏滚动条 |

---

## 9. 页面文件规范

### 页面模板结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>页面标题</title>
  <link rel="stylesheet" href="../assets/td.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    /* 页面专属样式（尽量少，优先用 td.css） */
  </style>
</head>
<body style="display:flex;flex-direction:column;height:100vh;overflow:hidden;background:var(--td-bg-page);">
<div id="app" v-cloak style="display:flex;flex-direction:column;height:100%;">

  <!-- NavBar -->
  <!-- 主体内容 -->
  <!-- TabBar（如有）-->
  <!-- 弹层（如有）-->
  <!-- Toast -->

</div>
<script src="../assets/js/vue.js"></script>
<script>
function _nav(p) {
  if (window.parent !== window) window.parent.postMessage({ navigate: p }, '*');
}

new Vue({
  el: '#app',
  data: { /* ... */ },
  methods: {
    showT(m) { this.toast = m; setTimeout(() => this.toast = '', 2000); }
  }
});
</script>
</body>
</html>
```

### 技术约束

| 规则 | 说明 |
|------|------|
| **Vue 版本** | 使用本地 `vue.js`（Vue 2.6.14），不使用 CDN |
| **Tailwind** | 仅 poster.html 等需要 gradient 工具类的页面按需引入 CDN |
| **不使用** | `.vue` 文件、`import` 语句、任何构建工具 |
| **移动优先** | viewport 固定，不响应式，以手机尺寸为准 |
| **滚动容器** | 主内容区 `flex:1; overflow-y:auto;`，加 `.no-sb` 隐藏滚动条 |
| **字体** | `font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", sans-serif` |

### NavBar 写法

```html
<!-- 白底导航栏（默认） -->
<div class="td-navbar">
  <div class="td-statusbar"></div>
  <div class="td-navbar-content">
    <div class="td-navbar-back" @click="_nav('prev-page')">
      <i class="fas fa-chevron-left"></i>
    </div>
    <span class="td-navbar-title">页面标题</span>
    <!-- 右侧操作（可选） -->
    <div class="td-navbar-action" @click="...">
      <i class="fas fa-xxx"></i>
    </div>
  </div>
</div>

<!-- Hero 导航栏（无 td-navbar，合并进 Hero 区域） -->
<div class="td-hero" style="...">
  <div style="height:20px;"></div>  <!-- 状态栏 -->
  <div style="height:44px;display:flex;align-items:center;padding:0 16px;">
    <div @click="_nav('prev-page')" style="color:#fff;cursor:pointer;">
      <i class="fas fa-chevron-left"></i>
    </div>
  </div>
  <!-- Hero 内容 -->
</div>
```

### 模拟数据规范

- 数据写在 Vue `data` 中，不外链 `data.js`（除非数据量很大）
- 日期统一使用 `2026-05-xx` 格式
- 用户信息：姓名「小明」，头像 `😊`，等级「Lv.4 习惯达人」
- 其他用户：小花 `🌸`、阿杰 `🦁`、小红 `🌺`、大牛 `🐂`、阿熊 `🐼`

---

## 10. 交付检查清单

完成原型后，按以下清单逐项验证：

### 结构验证

- [ ] `PRD.md` 已创建，包含页面清单和导航结构
- [ ] `app/index.html` 三栏布局可正常打开
- [ ] `app/assets/td.css` 已存在
- [ ] `app/assets/js/vue.js` 已复制
- [ ] 所有可预览界面（含二级/三级及 query 变体）已在左侧树中注册（`PAGES` 数组），且均有连续 `PXX` 编号
- [ ] 所有页面在右侧注解中已添加（`NOTES` 对象）

### 样式验证

- [ ] 所有页面引入 `td.css`，无彩色残留（绿/蓝/橙/红）
- [ ] NavBar 白底居中标题（Hero 页面除外）
- [ ] 按钮使用黑色（`td-btn-primary` 或 `td-btn-outline`）
- [ ] Tag 使用灰色系（`td-tag-default`/`success`/`warning`）
- [ ] Hero 使用黑色渐变 `#1a1a1a → #333`

### 交互验证

- [ ] 每个页面包含 `_nav()` 函数
- [ ] 所有导航跳转（返回箭头、Tab、按钮）均已接入 `_nav()`
- [ ] 微信 API 调用（分享、相册等）用 Toast 模拟
- [ ] 弹层有遮罩，点击遮罩可关闭
- [ ] Toast 2 秒后自动消失

### 右侧注解验证

- [ ] 每个页面有 4～10 条交互说明
- [ ] 交互说明格式为「触发 → 响应」
- [ ] 页面跳转链接可点击并实际切换页面

### 手机框验证

- [ ] 手机框外框 `406×860px`
- [ ] iframe 宽高写死为 `380×834px`
- [ ] 页面在手机框内无横向溢出

---

## 11. 层级页面、变体与侧边栏注册

### 定义

- **层级页面**：产品上属于二级 / 三级流程的界面（例如从首页进入的封闭打卡确认页），可以是独立 `pages/*.html`，也可以是同一文件的不同 **query 状态**（例如详情页「动态 / 状态 / 统计 / 人员」子视图）。
- **变体**：同一 `key` 下通过 `params` 区分的预览状态（空态、未登录、未加入等）。

无论产品在流程上是否「从父页进入」，**侧边栏必须按 [§2.1](#21-页面树全量暴露原则) 全量列出**，便于开发单独验收每一种界面。

### 何时用独立 HTML vs query 变体 vs 覆盖层

| 场景 | 推荐方案 |
|------|---------|
| 页面逻辑简单，与父页强耦合 | 父页内 overlay / Sheet |
| 复杂表单或独立导航栈 | 独立 `pages/*.html` |
| 同一页多种 UI 状态（空态、权限态） | 优先 **同一 HTML + query**，并在 `PAGES` 中为每种参数组合单独一行 |

### 注册规则（`PAGES`）

每一行包含：`id`、`key`、`params`、`label`、**`section`**、**`depth`**、**`pNum`（连续）**；`icon` 可选（建议仅分组首条主界面使用）。

```javascript
// 示例：主界面与衍生界面同属「今日打卡」分组，depth 区分层级，均为正式树节点
{ id:'home',           key:'home',             params:'', section:'今日打卡', depth:0, icon:'🏠', label:'今日打卡',           pNum:'01' },
{ id:'home#empty=1',   key:'home',             params:'empty=1', section:'今日打卡', depth:1, icon:'', label:'空态·无任务',   pNum:'02' },
{ id:'checkin-closed', key:'checkin-closed',   params:'', section:'今日打卡', depth:1, icon:'', label:'封闭模式·确认打卡',   pNum:'03' },
{ id:'checkin-open',   key:'checkin-open',     params:'', section:'今日打卡', depth:1, icon:'', label:'开放模式·确认打卡',   pNum:'04' },
```

```javascript
// NOTES 的 key 仍对应 iframe 加载的页面文件 key（与现有规则一致）
'checkin-closed': {
  interactions: [ ... ],
  nav: [ { key:'home', label:'返回今日打卡', dir:'back' } ],
}
```

### 侧边栏样式（层级仅作缩进）

- `depth` 每增加 1，左侧增加 **14px** 基础缩进（可在实现中微调，全项目统一即可）。
- 可选在图标位展示 **树连接线**（如 `├` `└`）或 `↳`，但不降低字号与对比度。
- **选中态**与一级页面一致：`background:#f5f5f5`、`border-left:3px solid #111`、`font-weight:600`。

### 导航规范（运行时）

- 返回箭头仍 `_nav('parent-key')` 回到业务父页；侧边栏「直达」不改变页面内返回逻辑。
- 父页进入子流程：`_nav('child-key')` 或带 `params`（若 Shell 支持 message 传参则一并约定）。

---

## 附录 A：常用组件代码片段

### A1 底部 Sheet 弹层

```html
<!-- 遮罩 -->
<div v-if="show" class="td-overlay" @click="show=false"></div>
<!-- 弹层 -->
<div v-if="show" class="td-sheet">
  <div class="td-sheet-handle"></div>
  <div class="td-sheet-header">
    <span class="td-sheet-title">标题</span>
    <div @click="show=false" style="color:var(--td-text-3);cursor:pointer;">
      <i class="fas fa-times"></i>
    </div>
  </div>
  <div class="td-sheet-body">
    <!-- 内容 -->
    <button class="td-btn td-btn-primary td-btn-block">主操作</button>
  </div>
</div>
```

### A2 Hero 区域

```html
<div class="td-hero" style="padding: 0 16px 16px; flex-shrink:0;">
  <div style="height:20px;"></div>
  <div style="height:44px;display:flex;align-items:center;">
    <div @click="_nav('back')" style="color:#fff;cursor:pointer;padding-right:12px;">
      <i class="fas fa-chevron-left" style="font-size:16px;"></i>
    </div>
  </div>
  <h1 class="td-hero-text" style="font-size:18px;font-weight:700;">标题</h1>
  <p class="td-hero-muted" style="font-size:12px;margin-top:4px;">副标题</p>
  <!-- 四格统计 -->
  <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin-top:12px;">
    <div v-for="s in stats" :key="s.l" class="td-hero-card">
      <div class="td-hero-text" style="font-size:17px;font-weight:800;">{{ s.v }}</div>
      <div class="td-hero-muted" style="font-size:10px;margin-top:2px;">{{ s.l }}</div>
    </div>
  </div>
</div>
```

### A3 日期分组列表

```html
<div v-for="grp in groups" :key="grp.date">
  <!-- 分组标题 -->
  <div style="display:flex;align-items:center;gap:6px;padding:6px 0 10px;">
    <div style="width:6px;height:6px;border-radius:50%;background:var(--td-text-1);"></div>
    <span style="font-size:12px;font-weight:700;color:var(--td-text-2);">{{ grp.label }}</span>
    <span style="font-size:11px;color:var(--td-text-3);">{{ grp.count }} 条</span>
  </div>
  <!-- 卡片列表 -->
  <div v-for="item in grp.items" :key="item.id" class="td-card" style="margin-bottom:8px;">
    <!-- 内容 -->
  </div>
</div>
```

### A4 Tab 切换

```html
<!-- Tab Bar -->
<div style="display:flex;background:var(--td-bg-card);border-bottom:1px solid var(--td-border);">
  <div v-for="t in tabs" :key="t.k"
    @click="cur=t.k"
    :style="'flex:1;padding:12px 0;text-align:center;font-size:14px;cursor:pointer;font-weight:'+(cur===t.k?'600':'500')+';color:'+(cur===t.k?'var(--td-text-1)':'var(--td-text-3)')+';border-bottom:2px solid '+(cur===t.k?'var(--td-text-1)':'transparent')+';'">
    {{ t.l }}
  </div>
</div>
```

### A5 选项选择底部弹层（打卡频率 / 枚举选择）

适用于字段值为有限枚举的场景（如打卡频率、补卡时间单位等），点击 Cell 弹出底部选择弹层。

```html
<!-- Cell 触发 -->
<div class="cell" @click="showSheet=true">
  <div class="cell-label">打卡频率</div>
  <div class="cell-value">{{ f.freq }}</div>
  <i class="fas fa-chevron-right cell-arrow"></i>
</div>

<!-- 弹层 -->
<div v-if="showSheet" class="td-overlay" @click="showSheet=false"></div>
<div v-if="showSheet" class="td-sheet" style="padding:0 0 8px;">
  <div style="padding:16px 16px 12px;border-bottom:1px solid var(--td-border);display:flex;align-items:center;justify-content:space-between;">
    <span style="font-size:15px;font-weight:700;color:var(--td-text-1);">选择打卡频率</span>
    <span style="font-size:13px;color:var(--td-text-3);cursor:pointer;" @click="showSheet=false">取消</span>
  </div>
  <div v-for="opt in options" :key="opt.val"
    @click="f.freq=opt.val; showSheet=false"
    style="display:flex;align-items:center;justify-content:space-between;padding:16px 20px;border-bottom:1px solid var(--td-border);cursor:pointer;"
    :style="f.freq===opt.val ? 'background:var(--td-bg-hover);' : ''">
    <div>
      <div style="font-size:15px;color:var(--td-text-1);" :style="f.freq===opt.val?'font-weight:600;':''">{{ opt.val }}</div>
      <div v-if="opt.desc" style="font-size:11px;color:var(--td-text-3);margin-top:2px;">{{ opt.desc }}</div>
    </div>
    <i v-if="f.freq===opt.val" class="fas fa-check" style="font-size:15px;color:var(--td-text-1);"></i>
  </div>
</div>
```

**规范要点**：
- 当前选中项背景 `var(--td-bg-hover)` + 右侧 `✓` 图标
- 每个选项可带副标题描述（`opt.desc`）
- 弹层标题行包含"取消"文字按钮
- 点击遮罩关闭，不改变已选值

---

### A6 固定名单姓名选择器（封闭模式专用）

适用于打卡时从封闭名单中选择自己姓名的场景。

```html
<!-- 选择器触发行 -->
<div class="name-picker-row" @click="openSheet">
  <span v-if="f.name" style="font-size:15px;color:var(--td-text-1);font-weight:500;">{{ f.name }}</span>
  <span v-else style="font-size:15px;color:var(--td-text-4);">请选择你的姓名</span>
  <i class="fas fa-chevron-right" style="font-size:13px;color:var(--td-text-4);"></i>
</div>

<!-- 底部弹层（带搜索） -->
<div v-if="showSheet" class="td-overlay" @click="closeSheet"></div>
<div v-if="showSheet" style="position:fixed;left:0;right:0;bottom:0;z-index:60;background:var(--td-bg-card);border-radius:var(--td-radius-l) var(--td-radius-l) 0 0;max-height:70vh;display:flex;flex-direction:column;">
  <!-- 标题行 -->
  <div style="display:flex;align-items:center;justify-content:space-between;padding:14px 16px 10px;border-bottom:1px solid var(--td-border);flex-shrink:0;">
    <span style="font-size:15px;font-weight:700;color:var(--td-text-1);">选择姓名</span>
    <span style="font-size:13px;color:var(--td-text-3);cursor:pointer;" @click="closeSheet">取消</span>
  </div>
  <!-- 搜索框 -->
  <div style="padding:10px 16px;flex-shrink:0;border-bottom:1px solid var(--td-border);">
    <input v-model="search" placeholder="搜索名字…"
      style="width:100%;height:36px;background:var(--td-bg-page);border:1px solid var(--td-border);border-radius:var(--td-radius-m);padding:0 12px;font-size:14px;outline:none;" />
  </div>
  <!-- 名单列表 -->
  <div style="overflow-y:auto;flex:1;">
    <div v-if="filteredNames.length===0" style="padding:28px 16px;text-align:center;color:var(--td-text-4);font-size:13px;">未找到匹配成员</div>
    <div v-for="n in filteredNames" :key="n"
      @click="f.name=n; closeSheet()"
      style="display:flex;align-items:center;justify-content:space-between;padding:14px 16px;border-bottom:1px solid var(--td-border);cursor:pointer;"
      :style="f.name===n?'background:var(--td-bg-hover);':''">
      <span style="font-size:15px;color:var(--td-text-1);" :style="f.name===n?'font-weight:600;':''">{{ n }}</span>
      <i v-if="f.name===n" class="fas fa-check" style="font-size:15px;color:var(--td-text-1);"></i>
    </div>
  </div>
</div>
```

**规范要点**：
- 弹层高度限制 `max-height:70vh`，超出内容可滚动
- 搜索框实时过滤（`includes` 匹配）
- 选中项高亮 + 打勾，点击即关闭弹层
- 未选中时提示文字使用 `--td-text-4`（最淡色）

---

### A7 可选启用区块（Toggle 控制展开/收起）

适用于某功能默认关闭、用户主动开启后展开详细配置的场景（如成员名单、允许补卡等）。

```html
<!-- Toggle 控制行（放在 cell-group 内） -->
<div class="cell" @click="f.enabled=!f.enabled">
  <div>
    <div class="cell-label">启用功能名称</div>
    <div class="cell-label-sub">开启后可进行XXX配置</div>
  </div>
  <div class="toggle-wrap" :class="f.enabled?'on':''" style="margin-left:auto;flex-shrink:0;">
    <div class="toggle-thumb"></div>
  </div>
</div>

<!-- 展开内容（仅启用时显示，紧接 Toggle 行） -->
<div v-if="f.enabled" style="padding:12px 16px;border-top:1px solid var(--td-border);">
  <!-- 子配置内容 -->
</div>
```

**规范要点**：
- Toggle 默认值：可选功能（名单、补卡）默认 `false`；必须开启的功能（循环打卡）默认 `true`
- Toggle 行整行可点击（`@click` 在外层 div 上）
- 展开内容与 Toggle 行同在一个 `cell-group` 内，通过 `border-top` 分隔
- 关闭时已填写的内容保留（不清空 `v-model`），重新开启后内容复现

---

### A8 AI 生成封面图预览卡

适用于表单中封面图字段，默认由 AI 根据主题自动生成，用户可重新生成或上传自定义图片。

```html
<!-- AI 封面预览 -->
<div :style="'border-radius:10px;height:116px;background:'+aiThemes[f.aiTheme].bg+';display:flex;flex-direction:column;justify-content:space-between;padding:11px 13px;overflow:hidden;'">
  <!-- 止观实验室 logo + AI badge -->
  <div style="display:flex;align-items:center;gap:5px;">
    <div style="width:20px;height:20px;border-radius:50%;background:rgba(255,255,255,0.92);display:flex;align-items:center;justify-content:center;">
      <span style="font-size:9px;font-weight:900;color:#222;">止</span>
    </div>
    <span style="font-size:11px;font-weight:600;color:rgba(255,255,255,0.88);">止观实验室</span>
    <span style="margin-left:auto;background:rgba(255,255,255,0.18);color:rgba(255,255,255,0.82);font-size:9.5px;font-weight:600;padding:2px 8px;border-radius:100px;border:1px solid rgba(255,255,255,0.25);">✦ AI 生成</span>
  </div>
  <!-- 项目名称（实时绑定） -->
  <div>
    <div style="width:22px;height:2px;background:rgba(255,255,255,0.4);border-radius:1px;margin-bottom:5px;"></div>
    <div style="font-size:15px;font-weight:700;color:#fff;line-height:1.3;text-shadow:0 1px 4px rgba(0,0,0,0.3);">{{ f.name || '打卡项目名称' }}</div>
  </div>
</div>

<!-- 操作按钮行 -->
<div style="display:flex;gap:8px;margin-top:9px;">
  <button @click="regenCover">🔄 重新生成</button>
  <button @click="f.coverMode='custom'">📷 上传自定义图片</button>
</div>
```

```javascript
// data
aiThemes: [
  { bg: 'linear-gradient(135deg, #1a1a2e 0%, #16213e 55%, #0f3460 100%)' }, // 深海蓝
  { bg: 'linear-gradient(135deg, #2d1b69 0%, #11998e 100%)' },               // 紫绿
  { bg: 'linear-gradient(135deg, #232526 0%, #3a3d40 100%)' },               // 炭灰
  { bg: 'linear-gradient(135deg, #1f4037 0%, #2c6e49 100%)' },               // 深绿
  { bg: 'linear-gradient(135deg, #3a1c71 0%, #d76d77 70%, #ffaf7b 100%)' }, // 暮橙
],

// method
regenCover() {
  this.f.aiTheme = (this.f.aiTheme + 1) % this.aiThemes.length;
  this.showT('AI 封面已重新生成');
},
```

**规范要点**：
- 封面预览实时反映当前填写的项目名称（`f.name`）
- 「重新生成」循环切换渐变主题（共 5 套），每次 +1 取模
- 「止观实验室」品牌 logo 始终出现在封面左上角
- 编辑模式下默认进入 `coverMode='custom'`（模拟已有封面）

---

## 附录 C：交互说明同步规则

> **强制规则**：每次修改任意页面（`pages/*.html`）时，必须同步更新 `app/index.html` 中对应页面的 `NOTES` 交互说明。

### 触发条件

| 页面变化类型 | 需更新的字段 |
|------------|------------|
| 新增可交互元素（按钮、手势、输入框、弹层） | `interactions` 追加对应条目 |
| 删除功能模块或交互 | `interactions` 移除对应条目 |
| 新增页面跳转 | `nav` 追加 `{ key, label, dir }` |
| 修改跳转目标 | `nav` 更新 `key` 和 `label` |
| 删除跳转路径 | `nav` 移除对应项 |

### NOTES 结构

```javascript
const NOTES = {
  'page-key': {
    interactions: [
      '触发方式 → 响应结果（含时间参数）',
    ],
    nav: [
      { key: 'target-page-key', label: '显示文字', dir: 'forward' | 'back' },
    ]
  }
}
```

### 格式规范

- 每条格式：**「触发方式」→「响应结果」**
- 必要时附加时间参数，如：`→ 延迟 1.2s 跳转详情页`
- 每页条数建议 **4～10 条**，聚焦关键交互

---

## 附录 B：参考项目

| 项目 | 路径 | 说明 |
|------|------|------|
| 打卡小程序 | `prototype-checkin-miniprogram/` | 参考实现：左侧树 **全量列出** 全部界面与变体，工具栏显示总界面数 |
| PRD 示例 | `App-Prototype-Demo-Creator/examples/prd-example.md` | PRD 写作参考 |
| 基础模板 | `App-Prototype-Demo-Creator/assets/templates/` | index.html / page.html 初始模板 |
