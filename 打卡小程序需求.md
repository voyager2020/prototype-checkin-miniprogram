# 打卡小程序 · 产品需求文档

> 本文档由 Claude Sonnet 4.6 根据交互原型（`prototype/indexdunhuang.html` v4）反向生成。
> 初版生成日期：2026-04-29　　**最后更新：2026-05-07（对齐页面代码逻辑 v4）**

---

## 目录

1. [产品概述](#1-产品概述)
2. [整体导航结构](#2-整体导航结构)
3. [页面功能详述](#3-页面功能详述)
   - 3.1 今日打卡（主页）
   - 3.2 打卡统计
   - 3.3 个人中心
   - 3.4 打卡项列表
   - 3.5 打卡项详情
   - 3.6 发起打卡
   - 3.7 生成海报
   - 3.8 全部打卡动态（checkin-feed）
   - 3.9 我的打卡记录（my-checkin-history）
   - 3.10 邀请落地页（invite-landing）
4. [核心交互流程](#4-核心交互流程)
5. [数据实体模型](#5-数据实体模型)
6. [非功能性要求](#6-非功能性要求)
7. [实现差异说明](#7-实现差异说明)

---

## 1. 产品概述

**产品名称**：打卡小程序（微信小程序）

**产品定位**：以社群为单位的习惯养成工具。用户可以创建或加入"打卡项"（即打卡挑战），通过每日打卡记录、进度可视化、排行榜和海报分享等机制，形成社群正向激励，帮助用户坚持好习惯。

**目标用户**：希望在群体监督下养成规律习惯的个人用户；以及有意愿组织习惯打卡活动的发起人（班主任、健身教练、自律社群运营者等）。

**第一阶段核心能力**：

| 能力 | 说明 |
|------|------|
| 每日打卡 | 用户在规定时段完成图片/文字/语音打卡 |
| 打卡项管理 | 创建、参与、查看进行中/已结束的打卡挑战 |
| 统计分析 | 热力图、完成率、连续天数、时段偏好等个人数据 |
| 社群激励 | 打卡排行榜、今日动态、未打卡催促、分享海报 |
| 海报生成 | 生成个性化打卡成就海报并分享至微信群/朋友圈 |

---

## 2. 整体导航结构

### 2.1 底部导航栏

底部导航栏仅保留两个主 Tab，保持界面简洁：

| Tab | 图标含义 | 对应主页 |
|-----|---------|---------|
| 今日 | 清单图标 | 今日打卡主页 |
| 统计 | 完成打勾图标 | 打卡统计页 |

> **设计决策**：「打卡项」列表和「个人中心」均从主页内入口进入，不占用底部 Tab 位置，降低新用户的认知负担。

### 2.2 页面层级

```
底部 Tab ─ 今日打卡（主页）
               ├─ 打卡弹层（打卡操作）
               │     └─ 打卡成功弹层 → 生成海报
               ├─ 打卡项列表（滑入子页）
               │     ├─ 我参与的
               │     ├─ 我发起的
               │     ├─ 打卡项详情（滑入子子页）
               │     │     └─ 催打卡弹层
               │     └─ 发起打卡（表单，滑入子子页）
               └─ 个人中心（头像入口，滑入子页）
                     └─ 生成海报（滑入子子页）

底部 Tab ─ 统计
               └─ 热力图日历（点击日期展开当日记录）
```

---

## 3. 页面功能详述

### 3.1 今日打卡（主页）

**页面路径**：默认首屏（Tab 1）

#### 顶部区域（Header）

| 元素 | 说明 |
|------|------|
| 问候语 | 显示"早上好 / 下午好 / 晚上好，[昵称]"，附当前日期 |
| 头像入口 | 右上角圆形头像，点击进入个人中心 |
| 汇总数据芯片 | 横排展示：已完成 N 项 / 待打卡 N 项 / 连续 N 天 |
| 打卡项入口 | 可点击的卡片，展示"进行中 N 项·今日待打卡 N 项"，点击进入打卡项列表页 |

#### 任务列表区域

- **待完成**区块：显示今日尚未打卡的项目卡片，每张卡片包含：
  - 项目图标（emoji）
  - 项目名称
  - 打卡时段 + 所属打卡项名称 + 成员数
  - "打卡"按钮（点击唤起打卡弹层）

- **已完成**区块：显示今日已打卡的项目卡片，卡片置灰，右侧显示绿色对勾及打卡时间。

#### 打卡弹层（底部上滑）

| 元素 | 说明 |
|------|------|
| 标题 | 当前打卡项名称 + 时段 |
| 姓名输入 | **必填**；自动带入上次填写内容，首次为空；为空时提交显示红框 + Toast"请先填写姓名" |
| 今日感想 | 多行文本输入框（0/200 字），**可选** |
| 打卡照片 | 最多 3 张，**可选**；预留照片缩略图位 |
| 确认打卡按钮 | 点击提交，关闭弹层，唤起打卡成功弹层 |

> **v4 变更**：移除打卡方式（图片/文字/语音）选择器；新增姓名字段（必填，自动带入）；按钮文案由"完成打卡"→"确认打卡"；照片上限由不限改为 3 张。

#### 打卡成功弹层（全屏遮罩）

| 元素 | 说明 |
|------|------|
| 成功图标 | 🎉 |
| 激励文案 | "你今天又完成了一项好习惯" |
| 连续打卡天数 | 大字展示当前连续打卡天数（含火焰图标） |
| 生成海报按钮 | 点击后直接跳转至海报生成页（连通流程，无需返回主页再进个人中心） |
| 邀请朋友按钮（主要） | 唤起微信分享面板，邀请好友加入该打卡项 |

> **v4 变更**：图标 ✅→🎉；"太棒了"→"邀请朋友"（主按钮）；激励文案更新。

---

### 3.2 打卡统计

**页面路径**：Tab 2（统计）

#### 顶部汇总数据

横排三格，展示：

| 指标 | 说明 |
|------|------|
| 连续天数 | 截至今日的最长连续打卡天数 |
| 累计天数 | 历史总打卡天数 |
| 本月完成率 | 本月已打 / 本月总应打 |

支持"月 / 年"维度切换。

#### 热力图日历

- 以 7 列（周日至周六）网格展示当月每天的打卡强度，颜色深浅代表打卡数量（0 ～ 3+ 项）。
- 今日单元格高亮。
- **可交互**：点击任意日期，在日历下方展开当日打卡记录卡片；卡片右上角有关闭按钮；再次点击其他日期时切换。
- 当日记录卡片包含：日期标题、各项打卡详情（项目名、打卡时间、打卡感想摘要）。

#### 各项目完成率（甜甜圈图）

每个参与中的打卡项显示一个圆环图，圆环内显示本月完成率百分比，图下标注项目名称。

#### 打卡时段偏好

水平条形图，展示用户打卡行为在各时段（06-08、08-10、12-14、21-23）的分布比例。

---

### 3.3 个人中心

**页面路径**：今日打卡主页 → 右上角头像入口

#### 个人信息区

| 元素 | 说明 |
|------|------|
| 头像 | 用户头像（emoji 占位） |
| 昵称 | 用户昵称 |
| 等级标签 | "⭐ Lv.N 习惯达人"（根据累计打卡天数自动评级，显示在昵称下方） |
| 设置入口 | 右侧图标，点击弹出 Toast"设置功能开发中"（当前占位实现） |

#### 数据概览

三格展示：

| 指标 | 说明 |
|------|------|
| 连续天数 | 当前连续打卡天数 |
| 累计天数 | 历史总打卡天数 |
| 参与打卡项目 | 当前参与中的打卡项目数量 |

#### 功能菜单

| 菜单项 | 说明 |
|--------|------|
| 生成我的海报 🎨 | 跳转至海报生成页（`/pages/poster/index`） |
| 我的打卡记录 📋 | 跳转至个人打卡记录页（`/pages/my-checkin-history/index`），按日期倒序分组展示 |

> **v4 变更**：新增"我的打卡记录"菜单项。设置、帮助等功能不在第一阶段实现。

#### 统计入口引导

页面下方放置引导文案："打卡统计详情请前往「统计」页查看热力图、完成率、时段分析"。

---

### 3.4 打卡项列表

**页面路径**：主页"打卡项入口"→ 滑入子页；或打卡项独立屏（Phone 4）

#### 页面顶部

- 标题：打卡项
- 右上角"发起打卡"按钮（含 + 图标），点击进入发起打卡表单

#### Tab 切换

| Tab | 内容 |
|-----|------|
| 我参与的 | 当前用户已加入的所有打卡项卡片列表 |
| 我发起的 | 当前用户创建的所有打卡项卡片列表 |

#### 我参与的 — 项目卡片

每张卡片包含：

| 元素 | 说明 |
|------|------|
| 项目图标（emoji）| 创建时选择 |
| 项目名称 | |
| 挑战标签 | 如"7天挑战""21天挑战"，颜色按周期长短区分 |
| 进度文字 | "第 N 天" |
| 状态徽章 | "待打卡"（橙色）或"已打卡 ✓"（绿色） |
| 完成率进度条 | 含百分比数字，颜色与项目类型对应 |
| 成员头像列表 | 最多显示 3 个头像 + "+N人" |

点击卡片进入打卡项详情页。

#### 我发起的 — 项目卡片

| 元素 | 说明 |
|------|------|
| 项目图标 + 名称 | |
| "我发起"标签 + 挑战类型 | |
| 状态徽章 | "进行中"（绿色）/"已结束"（灰色） |
| 成员数 | |
| 今日已打人数 | 绿色高亮 |
| 进度信息 | "第 N 天" 或 "已完成 N/N 天" |

---

### 3.5 打卡项详情

**页面路径**：打卡项列表 → 点击卡片

#### 详情头部（Hero）

绿色渐变背景，包含：

| 元素 | 说明 |
|------|------|
| 项目图标 + 名称 | |
| 挑战信息 | "N天挑战 · 连续模式 · 第N/N天" |
| 四格统计数据 | 成员数 / 今日已打 / 完成率 / 剩余天数 |
| 今日未打卡提醒条 | 显示今日未打卡人数及成员姓名摘要，右侧"催一催 →"按钮 |

#### 打卡排行榜

| 元素 | 说明 |
|------|------|
| 排行榜标题 | "打卡排行榜" |
| 排序维度切换 | 连续 / 累计 / 活跃（三选一） |
| 榜单项 | 名次（金/银/铜高亮）+ 头像 + 昵称（当前用户标注"我"）+ 连续天数 + 徽章 |

#### 今日打卡动态

展示今日已打卡成员的动态流：头像 + 昵称 + 打卡时间与感想摘要 + 打卡缩略图（可选）。

标题右侧显示"查看更多 →"，点击跳转至全部打卡动态页（`/pages/checkin-feed/index`，筛选当前项目）。

#### 催打卡弹层（底部上滑）

点击"催一催"按钮触发，包含：

| 元素 | 说明 |
|------|------|
| 标题 | "发送打卡提醒" |
| 说明文字 | 功能描述 |
| 今日进度条 | 已完成 N/N 人（含进度百分比） |
| 未打卡成员列表 | 头像 + 姓名 Chip 列表 |
| 提醒文字预览 | 自动生成的催打卡文字（含项目名、进度、未打卡人、截止时间） |
| 复制提醒文字 | 纯前端，使用 Clipboard API，无服务端依赖 |
| 发到微信群 | 调用 WeChat JSSDK `shareAppMessage`，服务端仅需签名接口 |

> **服务端要求说明**：
> - 复制文字：零服务端依赖，可立即实现。
> - 微信群分享：需要在服务端实现 JSSDK 签名接口（`wx.config`），无需存储任何业务数据。
> - **不实现**：Push 通知（需微信订阅消息权限 + 定时任务）、群机器人（需企业微信）— 留待后续阶段。

---

### 3.6 发起打卡

**页面路径**：打卡项列表 → "发起打卡"浮动按钮（`/pages/create-checkin/index`）

#### 表单内容

**封面图**（v4 新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| 封面图 | 图片上传 | 选填，比例 16:9；未上传时展示虚线占位区 |

**基本设置**

| 字段 | 类型 | 说明 |
|------|------|------|
| 项目名称 | 文本输入 | 必填 |
| 打卡描述 | 多行文本 | 选填（0/100 字） |

**活动时间**（v4 变更，取代原"挑战天数"预设选项）

| 字段 | 类型 | 说明 |
|------|------|------|
| 开始日期 | 日期选择器 | 必填 |
| 结束日期 | 日期选择器 | 必填，需晚于开始日期 |
| 总天数（计算值） | 只读展示 | 系统自动计算"共 N 天" |

**参与权限**

| 字段 | 类型 | 说明 |
|------|------|------|
| 加入方式 | 单选卡片 | 🌍 开放模式（任何人可参与）/ 🔒 封闭模式（邀请制加入） |
| 邀请截止日期 | 日期选择器 | 封闭模式下必填 |

> **v4 变更**：移除打卡时段、选择图标、挑战天数预设、打卡模式、打卡验证字段；新增封面图；活动时间改为开始/结束日期；参与方式文案由"公开/仅限邀请"改为"开放/封闭模式"。

**底部操作**

- 预览按钮（次要）
- 发起打卡按钮（主操作，绿色）

> **v4 变更**：按钮文案由"创建并邀请好友"改为"发起打卡"，同时新增"预览"次要按钮。

---

### 3.7 生成海报

**页面路径**：
- 个人中心 → "生成我的海报"
- 打卡成功弹层 → "生成海报"按钮（直通，跳过个人中心）

> **设计决策**：不保存用户生成的海报图片至服务器（节省存储成本），每次进入该页面实时生成。

#### 模板选择

横排可滑动的模板 Chip，当前支持：

| 模板 | 配色 |
|------|------|
| 晨光绿 | 绿色渐变（默认） |
| 暮光橙 | 橙红渐变 |
| 极夜紫 | 深紫渐变 |

点击切换，海报实时更新配色。

#### 海报预览（实时渲染）

| 元素 | 说明 |
|------|------|
| 用户信息行 | 头像 + 昵称 + 等级标签 |
| 主数字 | 大字展示连续打卡天数（如"28天"） |
| 项目名称 | 所属打卡项名称 |
| 日期 | 当日日期文字 |
| 个性签名/金句 | 可点击编辑，点击后变为可编辑文本框，失焦后保存 |
| 打卡照片区 | 上传本地图片后嵌入海报展示，默认为空（显示占位提示） |
| 二维码区域 | 模拟小程序码图标 + 邀请文字 |

#### 添加打卡照片

| 元素 | 说明 |
|------|------|
| 上传入口 | 可点击的上传区域，含相机图标 + 文字提示 |
| 文件类型 | 支持 JPG / PNG |
| 上传后 | 上传区域变为图片缩略图，海报照片区同步显示 |
| 技术实现 | 使用 FileReader API 读取本地文件并转为 Data URL，纯前端，无需上传至服务器 |

#### 底部操作按钮

| 按钮 | 说明 | 服务端依赖 |
|------|------|---------|
| 保存图片 | 将海报保存至手机相册 | 微信小程序 `wx.saveImageToPhotosAlbum`，无 |
| 发朋友圈 | 分享至微信朋友圈 | JSSDK，仅需签名接口 |
| 发给好友 | 以小程序消息卡片形式发送 | 标准小程序分享能力，无 |

---

### 3.8 全部打卡动态（checkin-feed）

**页面路径**：打卡项详情 → "查看更多 →"（`/pages/checkin-feed/index`）

| 元素 | 说明 |
|------|------|
| 日期分组标题 | 以圆点 + 日期（如"今天 · 2026-05-07"）分组，右侧显示当日条数 |
| 动态卡片 | 头像 + 昵称 + 所属打卡项 + 第 N 天 + 打卡时间 + 感想文字（可选）+ 打卡照片（可选，点击触发 `wx.previewImage`） |
| 空态 | 「暂无打卡记录」+ 文字引导 |

---

### 3.9 我的打卡记录（my-checkin-history）

**页面路径**：个人中心 → "我的打卡记录"（`/pages/my-checkin-history/index`）

| 元素 | 说明 |
|------|------|
| 日期分组标题 | 以圆点 + 日期 + 星期分组，倒序排列，右侧显示当日条数 |
| 记录卡片 | 打卡项图标 + 名称 + 所属打卡项 + 打卡时间 + 感想摘要（可选）+ 绿色对勾 |
| 空态 | 「暂无打卡记录」+ 引导前往首页打卡 |

---

### 3.10 邀请落地页（invite-landing）

**页面路径**：微信群内点击分享卡片 → 未参与该打卡项的用户

> 已参与该项目的用户点击分享卡片后，直接跳转至打卡填写页（`/pages/do-checkin/index`）。

#### 页面内容

| 元素 | 说明 |
|------|------|
| 渐变头部 | 项目图标 + 项目名称 + 挑战天数 / 第 N 天 / 成员数 |
| 统计卡片（小版） | 今日完成率 + 剩余天数（2 格展示） |
| 打卡规则 | 打卡时段 + 姓名必填说明 + 照片可选说明 |
| 当前成员 | 头像列表（最多 4 个 + "+N"溢出） |
| 底部操作 | "立即加入并去打卡"（主按钮），下方提示文案"加入后可在首页看到今日任务" |

> **备注**：该落地页在现有 pages 中还未独立实现，需新建页面并处理分享参数（`projectId`）的传递。

---

## 4. 核心交互流程

### 4.1 日常打卡流程

```
打开小程序
  → 今日打卡主页（查看待打卡列表）
  → 点击"打卡"按钮（唤起打卡弹层）
  → 填写姓名（必填，自动带入上次）+ 今日感想（可选）+ 打卡照片（可选，最多3张）
  → 点击"确认打卡"
  → 打卡成功弹层（展示连续天数）
  → [可选A] 点击"生成海报" → 直接进入海报生成页 → 分享海报
  → [可选B] 点击"邀请朋友" → 唤起微信分享 → 好友点击进入邀请落地页
```

### 4.2 查看打卡项统计并催打卡

```
主页
  → 点击"打卡项"入口
  → 打卡项列表（"我参与的" Tab）
  → 点击某个项目卡片
  → 打卡项详情（查看今日已打人数、未打卡人数）
  → 点击"催一催"
  → 催打卡弹层（查看未打卡成员列表）
  → 点击"复制提醒文字" 或 "发到微信群"
```

### 4.3 发起新打卡项

```
主页 → 打卡项入口
  → 打卡项列表页
  → 点击"+ 发起打卡"（浮动按钮）
  → 填写表单（封面图、名称、描述、开始/结束日期、参与权限）
  → 点击"发起打卡"
  → 创建成功 → 跳转至新项目详情页
  → 在"我发起的"列表卡片中点击"邀请好友" → 唤起微信分享
```

### 4.4 查看历史统计

```
底部 Tab → 统计
  → 查看热力图
  → 点击某一天
  → 展开当日打卡记录卡片
  → 查看各项打卡详情
```

### 4.5 从微信群进入打卡

```
好友在群内发送打卡分享卡片（含 projectId 参数）
  → 已参与该项目的成员点击
      → 直接跳转打卡填写页（/pages/do-checkin/index?projectId=xxx）
  → 未参与该项目的成员点击
      → 进入邀请落地页（展示项目信息、规则、当前成员）
      → 点击"立即加入并去打卡" → 加入项目 → 跳转打卡填写页
```

---

## 8. 原型维护规则

> 以下规则适用于 Web 原型（`打卡小程序/app/`）的日常迭代维护。

### 8.1 交互说明同步原则

**每次修改任意页面（`pages/*.html`）时，必须同步更新 `app/index.html` 中对应页面的 `NOTES` 交互说明。**

`NOTES` 对象位于 `index.html` 底部的 `<script>` 中，结构如下：

```javascript
const NOTES = {
  'page-key': {
    interactions: [
      '触发方式 → 响应结果',
      // ...
    ],
    nav: [
      { key: 'target-page', label: '目标页面说明', dir: 'forward' | 'back' },
    ]
  }
}
```

**更新触发条件：**

| 页面变化 | 需更新的 NOTES 字段 |
|---------|-------------------|
| 新增可交互元素（按钮、手势、输入框等） | `interactions` 追加对应条目 |
| 删除功能模块 | `interactions` 移除对应条目 |
| 新增页面跳转路径 | `nav` 追加对应链接 |
| 修改跳转目标 | `nav` 更新对应链接的 `key` 和 `label` |
| 删除页面跳转 | `nav` 移除对应链接 |

**格式规范：** 每条交互说明格式为 `「触发方式」→「响应结果」`，必要时附加时间参数（如"延迟 1.2s"）。
